[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "classification"
version = "0.1.0"
description = "TikTok multi-signal classification pipeline scaffolding"
authors = [{name = "AutoGenerated"}]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "boto3>=1.26",
]

[project.scripts]
classification-pipeline = "classification.cli:main"
classification-curate = "classification.cli_curate:main"
